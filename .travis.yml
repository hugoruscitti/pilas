language: python
python:
  - "2.7"
before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
install:
  - sh install_pyqt.sh
  - pip install mock
script:
  - python -m unittest discover pilasengine/tests '*.py'
virtualenv:
  system_site_packages: true
